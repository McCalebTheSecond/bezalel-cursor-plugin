# Bezalel Cursor plugin (moved)

This repository is **no longer the import target**.

Techtree's Cursor team marketplace and the Bezalel plugin package now live in the Techtree Partners org:

**https://github.com/Techtree-Partners/agent-skills**

Import that private repo from Cursor Dashboard → **Plugins** → Import from Repo. Set plugin `bezalel` to **Required** or **Default On**. Grant the Cursor GitHub App access if prompted.

Do not put Bezalel MCP tokens in a plugin repo. HTTP MCP stays on cursor.com/agents.
